# Generate Report Workflow

1. Run `npm run build`.
2. Review generated indexes in `data/generated/`.
3. Run `make render` when Quarto is installed.
4. Check draft labels before sharing public output.
