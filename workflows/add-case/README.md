# Add Case Workflow

1. Create `data/cases/<case-slug>/`.
2. Add `case.json`.
3. Add `source-pack.json`.
4. Add empty `passages.json`, `claims.json`, `interpretations.json`, `scores.json`, and `counterclaims.json`.
5. Run `npm run validate`.
6. Run `npm run generate`.
